# Astral Radar API
A high-performance API for radar signal processing and analysis.
## Installation
1. Clone the repository: git clone https://github.com/user/astral-radar-api.git
2. Install dependencies: npm install
3. Run the application: npm start
## API Endpoints
* GET /signals: Returns a list of radar signals
* POST /signals: Creates a new radar signal